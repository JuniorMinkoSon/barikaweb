"""API LocaConnecté (FastAPI) — couche HTTP propre et autonome.

Expose le référentiel métier (`backend.catalog`) et les moteurs universels
(`backend.engines`) : formulaires dynamiques, détection d'intention, devis,
matching/classement des prestataires.
"""
