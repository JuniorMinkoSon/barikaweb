# API LocaConnecté — dépendances backend (Python 3.11+)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic[email]>=2.6

# Base de données
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20           # driver SQLite async (dev local)
# aiomysql>=0.2           # driver MySQL async (prod)

# Auth
passlib[bcrypt]>=1.7
bcrypt<5                   # bcrypt 5+ incompatible passlib
python-jose[cryptography]>=3.3

# Tests
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27

# Phases suivantes :
# python-multipart>=0.0.9
# xgboost>=2.0           # optionnel : ranking ML
