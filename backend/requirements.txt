# API LocaConnecté — dépendances backend (Python 3.11+)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6

# Tests
pytest>=8.0
httpx>=0.27

# Phases suivantes (auth, base de données MySQL) :
# sqlalchemy>=2.0
# pymysql>=1.1            # driver MySQL
# passlib[argon2,bcrypt]>=1.7
# python-jose[cryptography]>=3.3
# python-multipart>=0.0.9
# xgboost>=2.0           # optionnel : ranking ML
